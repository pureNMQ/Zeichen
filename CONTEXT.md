# CONTEXT.md

本项目(贼船 Zeichen)的领域词汇表。实现细节不进本文件。

## 产品

- **贼船 Zeichen** — 本产品名。人类经 Web、agent 经 MCP 共用的项目协同管理工具;共享记忆(cognee)。

## 主体

- **用户(User)** — 人类成员,账号+密码登录,属于工作区。
- **Agent** — 独立用户账号类型(`is_agent`),由团队创建并归属团队;凭 API key 经 MCP 访问;权限按项目授予,与人类一视同仁;拥有独立记忆空间。
- **团队(Team)** — 工作区根节点,单例(一次部署只有一个团队);拥有项目与 agent。

## 工作区结构

- **项目(Project)** — 团队下的资源容器;需求、任务、文档、记忆空间都住在项目里;授权的最小单位。
- **记忆空间** — 每个主体的长期记忆载体(基于 cognee Dataset);agent 独立,按主体隔离。

## 领域概念

- **需求(Requirement)** — 单项目内的一个可验收变更单元;状态机:待办 → 实现中 → 已完成(侧路:已取消),完全自由流转(任意状态互转,无前置校验;状态全手动,任务状态变化不影响需求)。不支持父子层级。
- **任务(Task)** — 可执行事项,可指派给用户或 agent;状态机:待办 → 实现中 → 验收中 → 已完成(侧路:已取消),完全自由流转(无前置校验);派生任务带 requirement_id,独立任务可空。执行发生在外部 MCP 客户端,本工具只记录。
- **验收** — 需求/任务置"已完成"由任意有编辑权主体(人/agent)经通用改状态操作完成,无前置校验(带未决任务可直达),不强制留说明;activity 只记状态变更(action=status,摘要=旧态→新态),说明/理由经评论留存。
- **文档三子模块** — Wiki(自由页面)、词典(词条)、代码 API 参考(结构化的程序库符号)。
- **文档模块关联** — Wiki、词典与代码 API 参考相互隔离，不可形成跨模块父子关系或目录归属；跨模块只能通过引用关联。
- **代码 API 参考(Code API Reference)** — 项目的唯一 API 文档模块；记录程序库、命名空间和公开代码符号（类、接口、结构体、枚举、函数及成员）的定义和说明。代码结构由符号语义派生，不使用人工目录；HTTP Endpoint 及其请求/响应契约不属于本模块。
- **代码符号写入契约(Code Symbol Write Contract)** — 创建或更新代码符号时使用的统一语义数据；Web 面向人类提供表单与 Markdown 编辑，MCP 面向 agent 提供结构化输入，但两者表达相同的符号、定义、说明和版本语义。
- **类型符号(Type Symbol)** — 类、结构体、接口或枚举；可独立位于命名空间中，并且是其成员符号的唯一语义归属。
- **可调用符号(Callable Symbol)** — 顶级函数、构造函数或方法；以按顺序定义的参数、返回语义和异常语义描述一次可调用行为。
- **值符号(Value Symbol)** — 字段、属性或常量；以值类型及其特有的读写或常量语义描述。
- **成员符号(Member Symbol)** — 归属于一个类型符号的构造函数、方法、字段、属性或常量。成员不能脱离其所属类型存在；删除或恢复类型时，其成员随之原子地删除或恢复。
- **枚举项(Enum Member)** — 枚举定义内部按声明顺序排列的一项名称、可选显式取值和说明；枚举项不是代码符号，不具有独立页面、版本、引用或树节点，并随所属枚举整体保存、删除、恢复和版本化。
- **摘要(Summary)** — 每个可独立代码符号的一句简短说明，用于父级成员列表、代码树搜索结果和引用卡片；摘要独立维护，不能由完整说明的首行隐式推导。
- **完整说明(Remarks)** — 代码符号的 Markdown 详细说明，可包含多段内容和排版；不承担列表摘要的职责。
- **文档提及(Document Mention)** — 一个文档对另一文档建立的 `mentions` 引用；目标被软删除时保留但不可打开，恢复目标后自动重新有效。
- **Wiki 文档树(Wiki Document Tree)** — Wiki 模块内文档的父子层级；根 Wiki 没有父文档，每个子 Wiki 恰好归属一个父 Wiki。
- **模块目录(Module Directory)** — 仅存在于词典模块的持久化目录；可嵌套且只容纳词条。目录不是文档，不参与版本或引用。
- **记忆(记忆条目)** — agent 与系统留下的记录,分短期会话缓存与长期知识图谱两层。
