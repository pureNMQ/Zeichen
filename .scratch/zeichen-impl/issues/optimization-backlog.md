# 优化点收集(草稿合并):测试反馈 backlog

> 本文件为**草稿收集器**,非可执行 ticket。用户测试发现的优化点统一先落这里;
> 整理完成、逐项确认(含遗留决策)后再拆分回 `issues/NN-<slug>.md` 独立可执行票
> (拆分后按 issue-tracker.md 约定,`Status: open` 参与认领)。
>
> 优先级已定:**优化组全部先于 04-07 新增功能开发**。
>
> ✅ **已拆分执行(2026-08-02)**:逐项确认(09 方案 B:完全自由流转、删验收说明/校验/自动流转、需求四态/任务五态、MCP set_status 等)后拆分为 `issues/08-14`;本文件保留为草稿存档,不再参与认领。

---

## 08 侧边栏导航重构:需求/任务入口上收 + 当前项目切换

- **问题**:需求/任务在 `/projects/:projectId` 下 tab,层级太深
- **目标**:侧边栏分两区(团队功能:项目/成员/Agent 与 工作区:需求/任务);需求/任务上收为顶层 `/requirements`、`/tasks`;侧边栏底部(用户设置上方)加"当前项目"切换器,全局生效并持久化(localStorage + Context);无当前项目时默认选第一个可见项目
- **单入口(已定)**:需求/任务只经侧边栏进入;项目详情页保留,专注基础信息(名称/成员授权),移除需求/任务 tab;`useProjectRole` 上移全局 provider
- **依赖**:03

## 09 需求/任务状态交互增强:看板自由流转 + 直接验收 + 任务删除 + 拖拽动效

- **问题**:需求页不能直接改状态/拖拽(`canDrag={false}`);任务只许严格顺序流转、取消后不能恢复;验收必须进详情页(看板完成列禁放);任务无删除入口(后端 delete/restore 已有);拖拽不跟随鼠标、无插入动效
- **目标**:需求看板启用拖拽;任务流转放开;看板直接验收(弹验收说明对话框);任务卡片加删除入口(二次确认);dnd-kit `DragOverlay` 跟随 + 插入占位高亮
- **待确认**:状态机放开幅度——方案 A(推荐):非终态(backlog/in_progress/verifying)任意互转 + cancelled 可恢复,done 仍仅经验收进入;方案 B:完全自由。需求是否同步放开(建议同步)
- **依赖**:08

## 10 需求/任务状态下拉框:卡片与详情页直接调整状态

- **问题**:状态只能拖拽或详情页按钮流转,无直观下拉入口
- **目标**:卡片缩略图 + 详情页各提供状态下拉,列出合法目标状态(按 09 决策过滤);需填说明的流转(验收)选中后弹说明;权限按编辑权;与拖拽互为补充
- **依赖**:09(可达状态集合以其为准)

## 11 表单输入框 autocomplete 规范:杜绝浏览器凭据误判

- **问题**:新建需求/任务标题输入框被浏览器当凭据字段,触发保存密码/自动填充提示
- **目标**:所有非密码输入框显式 `autoComplete="off"`(或语义值),密码框保持 `current-password`/`new-password`;**立规范**:此后表单禁裸 Input,写入开发规范(AGENTS.md 等),后续票实现与 review 强制执行;排查全部表单不限于需求/任务
- **依赖**:03

## 12 成员管理角色列:锁定保护 + 下拉展开位置修复

- **问题**:首用户管理员角色可被改;任何人都能改自己角色;当前角色为"管理员"时下拉面板向上弹出,反直觉
- **目标**:
  - **守卫替换(已定)**:删除"最后一名管理员不可降级/不可移除"守卫;改为**首用户(bootstrap)必须是管理员且不可修改**(角色/移除均锁定),其他管理员可正常升降级;后端 `update_role`/`remove_member` 对齐,迁移加锁定标记(`is_bootstrap`/`role_locked`)
  - 角色列三类态:本人行禁用、首用户禁用、其余可选
  - 下拉固定向下展开(SelectContent side 配置或调整选项顺序)
  - 测试:前端三类态 + 后端守卫用例
- **依赖**:03

## 13 成员管理 / Agent 管理改为卡片式布局

- **问题**:两页表格布局,与全站卡片风格不一致
- **目标**:改为卡片网格/列表,信息与操作(角色下拉/移除;Agent 的 key 签发/回看/吊销)合理排布;复用 12 锁定态;响应式;测试随布局更新
- **依赖**:03

## 14 Agent API Key 管理交互:默认展开 + 图标操作 + 吊销即消失 + 签发可见明文

- **问题**(`agents.tsx` KeyDialog):
  - 打开对话框时 key 列表是空的,需再点"查看已有 key"才加载——应打开即直接展开显示全部 key
  - 回看/吊销是文字按钮("回看""吊销")——应改为图标按钮即可操作
  - 吊销后的 key 仍显示"已吊销"Badge 留在列表——应吊销后直接消失,不留存档
  - 首次签发时看不到明文 key——应签发成功后立即显示 token 并可复制(后端签发时明文可用,`issue_key` 返回 token;前端 `issueKey` 目前丢弃响应)
- **目标**:KeyDialog 打开自动加载并展开全部 key;回看(验证管理员密码后显示+复制)/吊销均为图标按钮;吊销后该项从列表移除(前端过滤;是否物理删除待实现确认,倾向仅隐藏,审计需要再议);签发成功弹明文 + 复制按钮(复用 `RevealedToken`)
- **依赖**:03

## 15 新建任务"关联需求"入口缺失

- **问题**:新建任务对话框只有标题+描述,无"关联需求"选择器;需求详情页也没有"新建任务"按钮——后端 `requirement_id` 支持已有(03),但 Web 端从未提供挂需求路径;对话框文案"任务可挂到需求下(承接拆解),也可作为独立任务"承诺了 UI 没有的能力
- **目标**:新建任务对话框加"关联需求"下拉(可空,列出当前项目未删除需求);需求详情页加"为该需求新建任务"入口(预选该需求);后端无需改动
- **依赖**:09(当前项目 Context)、10(下拉交互参考)
- ✅ **2026-08-02 已并入 10 票**执行
