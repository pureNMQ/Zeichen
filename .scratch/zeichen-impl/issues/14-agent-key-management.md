# 14 Agent API Key 管理交互:默认展开 + 图标操作 + 吊销即消失 + 签发可见明文

Status: resolved
Type: task
Blocked by: 03

## Question

实现 KeyDialog 交互改造(来自 optimization-backlog,已确认):

- 打开对话框自动加载并展开全部 key(不再需要先点"查看已有 key")
- 回看/吊销改为图标按钮
- 吊销后该项从列表消失(前端过滤 revoked_at;后端保留记录仅打标记,不物理删除,供审计)
- 签发成功立即显示明文 + 复制(复用 `RevealedToken`;`issueKey` 不再丢弃响应)
- **回看密码验证用独立小弹窗**;验证通过后明文展示处同时提供"复制"+"吊销"两个按钮
- 测试:前端 KeyDialog 交互(自动展开/吊销即消失/签发明文/独立验证弹窗)

## Answer

用户验收通过。Key 对话框打开即加载全部有效 Key；回看与吊销使用图标操作，吊销后立即从列表移除；签发与回看均展示可复制明文，回看密码验证采用独立弹窗，并已覆盖关键交互测试。

产出:key 全生命周期操作直观、吊销后列表干净、明文可见可复制可吊销。
